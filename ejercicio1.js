//1. Crear una función que devuelva el mayor de 2 números.

function encontrarMayor(numero1, numero2) {
    if (numero1 > numero2) {
        return numero1;
    } else {
        return numero2;
    }
}
